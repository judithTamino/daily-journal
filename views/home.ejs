<%- include('partials/header') -%>

<h1>Home</h1>
<p> <%= newHomeContent %> </p>

<% posts.forEach(post => { %>
    <h3> <%= post.title %> </h3>
    <p> 
        <%= post.body.substring(0, 100) + '...' %>
        <a href="/post/<%= post.title %>" + post.title>Read More</a> 
    </p>
   
<% }) %> 

<%- include('partials/footer') -%>
